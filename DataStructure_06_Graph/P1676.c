#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int factorial[1005] = {
        1,
        1,
        2,
    };

    int num;
    scanf("%d", &num);

    for (int i = 3; i <= num;)

        return 0;
}