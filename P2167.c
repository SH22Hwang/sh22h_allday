#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_SIZE 300

int main()
{
    int ary[MAX_SIZE][MAX_SIZE];
    int rows, cols, k, num1_x, num1_y, num2_x, num2_y;
    scanf("%d %d", &rows, &cols);
    for (int i = 0; i < rows; i++)
    {
        for (int j = 0; j < cols; j++)
        {
            ary[i][j]
        }
    }

    for (int i = 0;)

        return 0;
}